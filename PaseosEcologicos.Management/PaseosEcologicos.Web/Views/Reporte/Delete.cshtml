@model PaseosEcologicos.Model.vReporteDeCosumo

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>vReporteDeCosumo</legend>

    <div class="display-label">ReservacionId</div>
    <div class="display-field">@Model.ReservacionId</div>

    <div class="display-label">PaseoId</div>
    <div class="display-field">@Model.PaseoId</div>

    <div class="display-label">TituloDePaseo</div>
    <div class="display-field">@Model.TituloDePaseo</div>

    <div class="display-label">ClienteId</div>
    <div class="display-field">@Model.ClienteId</div>

    <div class="display-label">Cliente</div>
    <div class="display-field">@Model.Cliente</div>

    <div class="display-label">Celular</div>
    <div class="display-field">@Model.Celular</div>

    <div class="display-label">Correo</div>
    <div class="display-field">@Model.Correo</div>

    <div class="display-label">RepresentateId</div>
    <div class="display-field">@Model.RepresentateId</div>

    <div class="display-label">Representante</div>
    <div class="display-field">@Model.Representante</div>

    <div class="display-label">TelRepresentante</div>
    <div class="display-field">@Model.TelRepresentante</div>

    <div class="display-label">CorreoRepresentante</div>
    <div class="display-field">@Model.CorreoRepresentante</div>

    <div class="display-label">ServicioId</div>
    <div class="display-field">@Model.ServicioId</div>

    <div class="display-label">TipoDeServicio</div>
    <div class="display-field">@Model.TipoDeServicio</div>

    <div class="display-label">ServicioConsumido</div>
    <div class="display-field">@Model.ServicioConsumido</div>

    <div class="display-label">PrecioDeServicioConsumido</div>
    <div class="display-field">@String.Format("{0:F}", Model.PrecioDeServicioConsumido)</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


